[project]
name = "boti-sweet"
version = "0.1.0"
description = "Deployable skeleton suite for the Boti ecosystem: concentrates global configuration and wires in optional packages (ETL, BI, ...) based on client needs."
readme = "README.md"
authors = [
    { name = "Luis Valverde", email = "lvalverdeb@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "boti-sweet-config",
]

[project.optional-dependencies]
etl = ["boti-sweet-etl"]
bi = ["boti-sweet-bi"]
observability = ["boti-sweet-observability"]

[build-system]
requires = ["uv_build>=0.9.18,<0.10.0"]
build-backend = "uv_build"

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
boti-sweet-config = { workspace = true }
boti-sweet-etl = { workspace = true }
boti-sweet-bi = { workspace = true }
boti-sweet-observability = { workspace = true }
boti-sweet-dummy = { workspace = true }

[dependency-groups]
dev = [
    "boti-sweet-dummy",
    "mypy>=1.14.0",
    "pytest>=9.0.3",
    "pytest-asyncio>=1.3.0",
    "pytest-cov>=6.0.0",
    "ruff>=0.11.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests", "packages"]

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "C4", "SIM"]
ignore = ["B008"]

[tool.mypy]
python_version = "3.13"
strict = true
ignore_missing_imports = true
exclude = ["tests/", "examples/", "packages/[^/]+/tests/"]
